{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% if app.user %}
        {{ include('inc/editProfile.html.twig') }}
    {% else %}
        {{ include('inc/register.html.twig') }}
    {% endif %}
{% endblock %}
